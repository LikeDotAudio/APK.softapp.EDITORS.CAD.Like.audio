# Architecture Notes & Technical Specs — CAD.Like.audio

## 🛠 Integration Details
Uses 5-level relative path resolution (`../../../../../`) to load `brand.json` from `APK:BareMetal`. Built with Vite and TypeScript.
